<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
        }

        .cardContainer {
            display: grid;
            gap: 10px;
            grid-row: 4px;
            grid-column: 4px;
            grid-template-columns: 1fr 1fr 1fr;
        }

        .cardBox {
            justify-self: center;
            display: grid;
            justify-items: center;
            border: 1px solid  #ffe26a;
            border-radius: 4px;
            padding: 10px;
            width: 250px;
            height: 200px;
            background-color: #c7ddcc;
        }

        .name {
            font-family: 'Abril Fatface', cursive;
            color: #16123f;
        }
        .role{
            color: green;
            
        }
        .contact{
            color: rgb(160, 165, 2);
        }
        .iconBox {
            background: hsla(102, 43%, 72%, 1);

            background: linear-gradient(90deg, hsla(102, 43%, 72%, 1) 30%, hsla(167, 44%, 62%, 1) 100%);

            background: -moz-linear-gradient(90deg, hsla(102, 43%, 72%, 1) 30%, hsla(167, 44%, 62%, 1) 100%);

            background: -webkit-linear-gradient(90deg, hsla(102, 43%, 72%, 1) 30%, hsla(167, 44%, 62%, 1) 100%);

            filter: progid: DXImageTransform.Microsoft.gradient(startColorstr="#ABD699", endColorstr="#75C9B7", GradientType=1);

            filter: progid: DXImageTransform.Microsoft.gradient(startColorstr="#FFE26A", endColorstr="#2E703B", GradientType=1);
            display: grid;
            align-self: center;
            justify-self: center;
            width: 110px;
            height: 110px;
            border-radius: 50%;
        }

        .icon {
            border: 4px solid linear-gradient(90deg, hsla(48, 100%, 71%, 1) 30%, hsla(132, 42%, 31%, 1) 100%);
            ;
            border-radius: 50%;
            justify-self: center;
            align-self: center;
            width: 100px;
            height: 100px;

        }

        .header {
            display: grid;
            margin-top: 0;
            height: 150px;
            background-color: #abd699;
        }

        .headline {
            align-self: center;
            justify-self: center;
            font-weight: 900;
            margin-left: 30px;
            color: whitesmoke;
            font-family: 'Cinzel', serif;
            font-size: 72px;
        }

        .subheadline {
            text-align: center;
            font-size: 2rem;
            padding: 20px 0;
            margin-bottom: 20px;
            background-color: #75c9b7;
            color: #16123f;
        }
    </style>
    <title>Document</title>
</head>

<body>

    <body>
        <header class="header">
            <div class="logo"><img src="./UserData/logo_ADSC_Times.jpg" alt=""></div>
            <h1 class="headline">ADSC Times</h1>
        </header>
        <h3 class="subheadline">Team Members</h3>
        <div class="cardContainer">
            <!-- Create an <li> element for each user in users -->
            <% data.data.forEach((user)=> { %>
                <div class="cardBox">
                    <div class="iconBox"><img class="icon" src="http://localhost:8080/profile/<%= user.profilePic%>">
                    </div>
                    <div class="name">
                        <%= user.name %>
                    </div>
                    <div class="role">
                        <%= user.role %>
                    </div >
                    <div class="contact">Mob : <%= user.mobileNo %>
                    </div>
                </div>
                <% }); %>
        </div>
    </body>

</body>

</html>